'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {evaluate}=require('../domain');const ctx={actor:'creator-1'};const base={ownerId:'creator-1',rightsConfirmed:true,subjectConsent:true,moderationApproved:true,scriptVersion:'script-v3',assetRefs:['asset:2','asset:1'],syntheticMedia:true,targets:[{provider:'youtube',accountRef:'acct:yt',publishAt:'2026-08-01T12:00:00Z',caption:'Launch'},{provider:'tiktok',accountRef:'acct:tt',publishAt:'2026-08-01T13:00:00Z',caption:'Short'}]};test('publishing manifest is deterministic and provider typed',()=>{const out=evaluate(base,ctx);assert.deepEqual(out,evaluate(base,ctx));assert.deepEqual(out.result.publishingManifests.map(x=>x.provider),['tiktok','youtube']);assert.equal(out.result.contentState,'human_review_required');assert.equal(out.result.disclosureRequired,true);});test('rights, consent, moderation and unsupported providers fail closed',()=>{const out=evaluate({...base,rightsConfirmed:false,subjectConsent:false,moderationApproved:false,targets:[{provider:'unknown'}]},ctx);assert.ok(out.errors.length>=4);});
